server:
    port: 6969

spring:
    datasource:
        url: jdbc:mysql://localhost:3309/n8n_db?useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC&connectionCollation=utf8mb4_unicode_ci&createDatabaseIfNotExist=true
        username: root
        password: ${DB_PASSWORD:admin}
        driver-class-name: com.mysql.jdbc.Driver

    jpa:
        hibernate: 
            ddl-auto: create
        show-sql: true
        properties: 
            hibernate:
                format-sql: true